[project]
name = "tourist-planner"
version = "0.1.0"
description = "Local, evidence-aware personal travel planner"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.54,<2",
    "xlsxwriter>=3.2.9",
]

[tool.uv]
package = false
